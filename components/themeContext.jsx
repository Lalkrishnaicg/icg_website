// "use client";
// import { ThemeProvider } from "@mui/material";
// import { ThemeProviderWrapper } from "./themeClient";

// // This ensures ThemeProvider runs only on the client side
// export default function ThemeProviderClient({ children }) {
//   return <ThemeProviderWrapper>{children}</ThemeProviderWrapper>;
// }
